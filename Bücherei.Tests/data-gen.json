{
    "Buechereien": [
        "{{repeat(5)}}",
        {
            "Id": "{{index()}}",
            "Name": "{{firstName()}} {{lorem(1, \"words\")}}",
            "Authoren": [

            ]
        }
    ],
    "Authoren": [
        "{{repeat(100)}}",
        {
            "Id": "{{index()}}",
            "Name": "{{firstName()}} {{surname()}}",
            "Buecher": [
                "{{repeat(20, 80)}}",
                "{{integer(1,499)}}"
            ],
            "BuechereiId": "{{integer(0,5)}}"
        }
    ],
    "Buecher": [
        "{{repeat(500)}}",
        {
            "Id": "{{index()}}",
            "Title": "{{lorem(1, \"words\")}} {{city()}}",
            "AutorId": "{{integer(0,5)}}"
        }
    ]
}